alphabet = "abcdefghijklmnopqrstuvwxyz"
op ceaserify(string, shift) do {
    res = ""

    for char : string do {
        if char == " " do {
            res += " "
            continue
        }
        toGet = (alphabet.indexOf(char) + shift) % 26
        res += alphabet ? toGet
    }

    return res
}

print(ceaserify("hello", 2))
