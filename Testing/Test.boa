import "requests"

channelID = null
authorization = null
content = null

payload = {
    "content": content
}
header = {
    "authorization": authorization
}

res = requests.post("https://discord.com/api/v9/channels/" + channelID + "/messages", payload, header)