import "sqlite3"
import "/Users/preetithorat/Documents/BoaEnvironment/CLIify.boa"

db = sqlite3.open("lolapalooza.db")
db.setCallback(op(names, vals) do {
    for i = 0 to lenOf(names) - 1 do {
        print(names[i] + " = " + vals[i])
    }
    print()
})
sql = "SELECT * from COMPANY"
db.exec(sql)